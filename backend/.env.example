# Configuración de PostgreSQL
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=tu_password
PGDATABASE=solicitudes_db

# Puerto del servidor Express
PORT=4000

# Autenticación
JWT_SECRET=cambia_esto_por_una_cadena_larga_y_secreta
JWT_EXPIRA_EN=8h

# Motor de notificaciones (actuador) -----------------------------
# EMAIL_PROVIDER: "resend" | "smtp" | (vacío = solo consola, desarrollo)
EMAIL_PROVIDER=
EMAIL_FROM="Solicitudes Escolares <no-responder@escuela.edu>"

# Si EMAIL_PROVIDER=resend
RESEND_API_KEY=

# Si EMAIL_PROVIDER=smtp
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
